// Because function are objects we can put then 
// in a variable or store 10 in an Array. we can 
// even pass them around as arguments

const squared = function (num){  // the function is stored in a variable
    return num * num;          // and does not have a name
}
squared(7); // 49



function add( x , y) {
    return x + y;
}


const total2 = function (x, y ){
    return x + y;
}



  